doctype html
html 
    head
        block head
            meta(charset='UTF-8')
            meta(name='viewport' content='width=device-width, initial-scale=1.0')
            meta(http-equiv="Content-Security-Policy" content="default-src * 'unsafe-eval' 'unsafe-inline'; img-src 'self' data:")
            link(rel='stylesheet' href='/css/style.css')
            link(rel='stylesheet' href='https://fonts.googleapis.com/css?family=Lato:300,300i,700')
            link(rel='shortcut icon' type='image/png' href='/img/favicon.png')
            
            title Natours | #{title}
    
    body
        //HEADER
        include _header


        //CONTENT
        block content 
            h1 jerry
        
        //- FOOTER
        include _footer
        
        
        script(src='/js/bundled/index.js')
        script(src='https://js.stripe.com/v3/')
        

        //- script(src='https://cdnjs.cloudflare.com/ajax/libs/axios/1.3.4/axios.min.js') so you need to install this using npm

        //- script(src='/js/login.js')
        
        //- script(src='/js/mapbox.js')
        //- script(src='/js/bundled/index.js')
      

//- install a parcel to bundle all the files. like js/login.js and mapbox.js in one file npm install --save-dev parcel
//- /There is Parcel 2 available, more on that https://v2.parceljs.org,

//- 1. Installation:
//- npm install parcel@next --save-dev

//- 2. Then you have to add this piece of code below "main": "app.js", in package.json as shown below, so parcel doesn't bundle into your app.js file.

//- "main": "app.js",
//- "targets": {
//-   "main": false
//- },
//- 3. Next you need to put the command in scripts part of package.json

//- "build": "parcel build ./public/js/index.js --dist-dir ./public/js/bundled"

//- 4. Now your script bundled script will have a path ./public/js/bundled/bundle.js, so you need to this line to base.pug:



   
 
